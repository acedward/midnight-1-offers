{
  "schemaVersion": "artifact-decision-matrix-v2",
  "pinsDigest": "467e6e9bb1ddebd11f2c0dea341b2fe584daa82ca90ef82d205664d9a96a73eb",
  "frozenOn": "2026-09-01",
  "note": "Frozen artifact-selection contract for every external dependency of this Midnight 1.x demo. Each field was re-derived from a live source on the freeze date. scripts/verify-artifact-decisions.sh enforces it offline. Do not edit a digest, SHA or hash to make a build pass: re-verify against the source first, then regenerate pinsDigest with --update-pins so the change is visible in review. Schema v2 differs from the 2.x sibling's v1 by adding `sources` (git-pinned source builds) and `toolchains`, and by replacing the warehouse proof-data generation with a pre-warm strategy.",
  "selectionOrder": [
    "official-oci",
    "warehouse-binary",
    "exact-oci-mirror",
    "source-build"
  ],
  "selectionRule": "Prefer a good official multiarch OCI image pinned by index digest. Otherwise use the verified official warehouse binary inside a thin local image. Otherwise mirror an unreliable upstream OCI image exactly into an owner-controlled registry. Only build from source when no reusable executable artifact exists.",
  "linuxArchitectures": [
    "amd64",
    "arm64"
  ],
  "forbiddenAssetSubstrings": [
    "macos",
    "darwin",
    "windows",
    "-standalone_"
  ],
  "warehouse": {
    "repository": "effectstream/binaries",
    "releaseTag": "0.3.120",
    "releaseId": 270761136,
    "assetCount": 97,
    "catalogCommit": "42c1b610ea1cbafd3eaf4c95901610d79541091b",
    "catalogPath": "metadata/releases/0.3.120.json",
    "mutable": true,
    "distributionTier": "development-only",
    "warning": "DEVELOPMENT ONLY - NOT FOR PRODUCTION USE. Release 0.3.120 is mutable; verify every downloaded SHA-256 against this file before installation or execution."
  },
  "components": [
    {
      "id": "midnight-node",
      "version": "1.0.0",
      "inArtifactNormalizationScope": true,
      "decision": "official-oci",
      "reason": "A good official multiarch image exists for the exact required version. Repacking it under another registry owner would add risk and no value.",
      "oci": {
        "repository": "docker.io/midnightntwrk/midnight-node",
        "readableTag": "1.0.0",
        "indexDigest": "sha256:ede01da35e982b6a4b85461ad8492ae2753ef14246fba33c8039b782aa8e39fb",
        "indexMediaType": "application/vnd.docker.distribution.manifest.list.v2+json",
        "platforms": {
          "linux/amd64": {
            "manifestDigest": "sha256:666e5bc90814618423c33e11088e94b51c9fd62e886a614f996cddaec1a6623c",
            "configDigest": "sha256:530d6b0a9dc18fb0f5734d1c977a2dade24a4c29c531bb90e558e9a3704c09f9"
          },
          "linux/arm64": {
            "manifestDigest": "sha256:c3e46f8beff308b352813cd8904f6480c331504e09060abd671b8a32e1255220",
            "configDigest": "sha256:1a9fdc745f5731b1415c8fe07478f9b634f624e6ffca36bbec3f7bdebc8db571"
          }
        }
      }
    },
    {
      "id": "indexer-standalone",
      "version": "4.3.3",
      "inArtifactNormalizationScope": true,
      "decision": "official-oci",
      "reason": "Unlike the 4.4.0-rc.3 the 2.x sibling needs, 4.3.3 IS published as a good official multiarch image, so this repository neither compiles it nor installs a warehouse binary into a thin local image. Both are strictly worse when an official index exists.",
      "oci": {
        "repository": "docker.io/midnightntwrk/indexer-standalone",
        "readableTag": "4.3.3",
        "indexDigest": "sha256:03afd079b00bcd229df29a24771439c5e7695c339cd89216d0763ce40731cc4b",
        "indexMediaType": "application/vnd.oci.image.index.v1+json",
        "platforms": {
          "linux/amd64": {
            "manifestDigest": "sha256:07de7d3b615a95cf0fa693557d69446cb4fce0d52dc6d2182d719b20a61c478e",
            "configDigest": "sha256:e22afd54ca3afb622e3a0f3440b495f8e768476c15a81f77325f80429f20ff09"
          },
          "linux/arm64": {
            "manifestDigest": "sha256:ff3f511ab81e7e337b76dd794cfbe0da8d22c43b915ac457557f937bb6e5f868",
            "configDigest": "sha256:9d76917aee7574d6085a2532b4d694dc0862e9e2aa1966ddd3916fffe34e8f2b"
          }
        }
      },
      "$comment": [
        "The index also carries two unknown/unknown attestation manifests (sha256:57b5182d… and",
        "sha256:1452f338…), which are in-toto provenance attachments referencing the two real",
        "platform manifests, not runnable platforms. They are deliberately NOT recorded as",
        "platforms: `platforms` means 'what this stack may run', and an attestation manifest is",
        "not that.",
        "",
        "4.3.3 serves an IDENTICAL GraphQL schema at /api/v3/graphql and /api/v4/graphql. Both",
        "paths are asserted by verify.sh so a future version that drops one cannot break half",
        "the stack silently."
      ]
    },
    {
      "id": "proof-server",
      "version": "8.1.0",
      "inArtifactNormalizationScope": true,
      "decision": "official-oci",
      "reason": "A good official multiarch image exists for the ledger-v8 proof server. There is exactly ONE proof server in this repository: the second, experimental (zkir-v3) build the 2.x sibling mirrors exists only for its AA profile, which this repository does not have. Nothing here compiles with --feature-zkir-v3, so no second variant, no separate pin, and no mirror record are needed.",
      "oci": {
        "repository": "docker.io/midnightntwrk/proof-server",
        "readableTag": "8.1.0",
        "indexDigest": "sha256:801bbc0340e9e96f16735f77b523f23c7459e3359842f7c79c2c53f4e994d531",
        "indexMediaType": "application/vnd.docker.distribution.manifest.list.v2+json",
        "platforms": {
          "linux/amd64": {
            "manifestDigest": "sha256:829d02876b346fe773d3d17419a38aadb9fafc6653603afbb3f16672bc122bd4",
            "configDigest": "sha256:6e1ffb4a816a751443762ff5e8400852a4a1e38ba764b0a0ec4cc520cddbcdce"
          },
          "linux/arm64": {
            "manifestDigest": "sha256:1288b69dde74551f8bd9a50eba0609e468bd7d3874d3e00d7dd0f45f35800797",
            "configDigest": "sha256:bda885e7f6b7f46fed33d514a5b2166541cf2290e72e2094edb2440bdfd4ace6"
          }
        }
      }
    },
    {
      "id": "celestia-appd",
      "version": "6.4.10",
      "inArtifactNormalizationScope": true,
      "decision": "warehouse-binary",
      "reason": "The combined local Celestia devnet runtime is carried over unchanged in identity from midnight-2-offers: these two versions are exactly what @effectstream/celestia@0.103.1 — a kernel-main dependency — vendors, so this is the devnet the offer-files kernel was developed against. Both binaries come from the warehouse by TARGETARCH; Linux arm64 IS published, so there is no platform pin.",
      "assets": {
        "linux/amd64": {
          "name": "celestia-appd-linux-amd64-v6.4.10.tar.gz",
          "assetId": 378640596,
          "outerSize": 198844329,
          "outerSha256": "fa1826187a91514c6d506e22f1fcce25afbd551b98458caf899801b662a0c8da",
          "format": "tar.gz",
          "memberPath": "celestia-appd",
          "memberSize": 285614960,
          "memberSha256": null,
          "installMode": "0755",
          "catalogProvenance": "legacy-unverified",
          "officialEquality": {
            "required": true,
            "reason": "The catalog row truthfully records legacy-unverified with null source and null member hashes. Those nulls must never be back-filled. Equality is instead proven directly against the official release.",
            "repository": "celestiaorg/celestia-app",
            "releaseTag": "v6.4.10",
            "releaseId": 282480015,
            "assetName": "celestia-app_Linux_x86_64.tar.gz",
            "assetId": 349955092,
            "assetSize": 198844329,
            "assetSha256": "fa1826187a91514c6d506e22f1fcce25afbd551b98458caf899801b662a0c8da",
            "checksumsAssetId": 349955278,
            "checksumsSha256": "960c02e97215f6c802e0992a64458b3f3df04cf2fb56f2f9e802a204a7a9e5c1",
            "verifiedOn": "2026-08-31"
          }
        },
        "linux/arm64": {
          "name": "celestia-appd-linux-arm64-v6.4.10.tar.gz",
          "assetId": 534358491,
          "outerSize": 180685852,
          "outerSha256": "52cc9d59f9db5e3d2b7de91008c808f46ba319922db4a39404735b0a5dd6a76b",
          "format": "tar.gz",
          "memberPath": "celestia-appd",
          "memberSize": 262644248,
          "memberSha256": "bdeec5aefadecd3b517545347110ea1f9e95dfabb5a287979c1a3fb6626db91b",
          "installMode": "0755",
          "catalogProvenance": "known",
          "sourceProvenance": {
            "repository": "celestiaorg/celestia-app",
            "commit": "0b26ea0cf0b52406d6004cef4a36e13c30ddca85",
            "method": "rename-only",
            "upstreamAssetName": "celestia-app_Linux_arm64.tar.gz",
            "upstreamAssetId": 349955149
          }
        }
      }
    },
    {
      "id": "celestia-node",
      "version": "0.28.4",
      "inArtifactNormalizationScope": true,
      "decision": "warehouse-binary",
      "reason": "Same as celestia-appd. Note the installed executable is named 'celestia', not 'celestia-node'.",
      "assets": {
        "linux/amd64": {
          "name": "celestia-node-linux-amd64-v0.28.4.tar.gz",
          "assetId": 378640594,
          "outerSize": 78842331,
          "outerSha256": "bb8b9fd2ac859945fdf2f8c84ba038f75e01be54e56d1a946cbbd4b540f120d9",
          "format": "tar.gz",
          "memberPath": "celestia",
          "memberSize": 190846060,
          "memberSha256": null,
          "installMode": "0755",
          "catalogProvenance": "legacy-unverified",
          "officialEquality": {
            "required": true,
            "reason": "The catalog row truthfully records legacy-unverified with null source and null member hashes. Those nulls must never be back-filled. Equality is instead proven directly against the official release.",
            "repository": "celestiaorg/celestia-node",
            "releaseTag": "v0.28.4",
            "releaseId": 265784044,
            "assetName": "celestia-node_Linux_x86_64.tar.gz",
            "assetId": 321528009,
            "assetSize": 78842331,
            "assetSha256": "bb8b9fd2ac859945fdf2f8c84ba038f75e01be54e56d1a946cbbd4b540f120d9",
            "checksumsAssetId": 321528021,
            "checksumsSha256": "10b730253822bff3be9517f649d56b26ebfa192f23a40b9a0b1b2313a55425ec",
            "verifiedOn": "2026-08-31"
          }
        },
        "linux/arm64": {
          "name": "celestia-node-linux-arm64-v0.28.4.tar.gz",
          "assetId": 534359261,
          "outerSize": 71184641,
          "outerSha256": "09eb0505c5265bb08dfd09f14aa397516efd89d7b8f120e06f133d9e387ad50c",
          "format": "tar.gz",
          "memberPath": "celestia",
          "memberSize": 176946394,
          "memberSha256": "b38154ba4ec52ba784c4fd1e52af41fb9fb78e1a928f20cf9264d837a7ad6d2e",
          "installMode": "0755",
          "catalogProvenance": "known",
          "sourceProvenance": {
            "repository": "celestiaorg/celestia-node",
            "commit": "52cd8b52ec031bb9e3b4e476e0b159db2053384c",
            "method": "rename-only",
            "upstreamAssetName": "celestia-node_Linux_arm64.tar.gz",
            "upstreamAssetId": 321528010
          }
        }
      }
    }
  ],
  "sources": [
    {
      "id": "offerfiles-kernel",
      "role": "the offer-files kernel, batcher and contract-deploy one-shot — one image, three entrypoints",
      "acquisition": "anonymous-git-fetch",
      "repository": "https://github.com/effectstream/zswap-offerfiles-kernel.git",
      "branch": "main",
      "ref": "c293ebd57937c0065663b08b2c244438be8989a5",
      "envVar": "KERNEL_REF",
      "verifiedOn": "2026-09-03",
      "reason": "The 1.x line: @effectstream/* 0.103.1, ledger-v8 8.1.0, mip-zswap-offer 0.3.0, contract pragma 'compact compile +0.30.0', /v1 API. RE-PINNED TO main's new HEAD IN 00011 PR A — THE WHOLE-COIN LINE. Five first-parent merges past the phase H2 pin, all on main and all still v8 (verified: root package.json and packages/solver-core/package.json carry ledger-v8 and no ledger-v9 at this ref): #61 seeds sNight as a default known token (it seeds the PREVIEW contract's colour, which cannot exist on an undeployed devnet — see the SNIGHT note in $comment and docs/KNOWN-LIMITATIONS.md), #62 ledger-v9 merged by mistake and REVERTED by #64 (net zero; re-opened upstream as #65, still OPEN and NOT merged as of this pin), #63 BREAKING: known_tokens.decimals now DEFAULTS to 6 instead of 0, every seeded row is 6, and every faucet mints WHOLE COINS scaled by 10^6 (the deploy one-shot's MINT_AMOUNT is unchanged at 1_000_000_000 base units but now MEANS 1 000 coins), #66 the offer-poster GIVE_MIN/GIVE_MAX/SIZE_SEED range (unused until 00011 PR C). BREAKING FOR AN EXISTING postgres VOLUME: 000-init.sql has no IF NOT EXISTS and runs once against an empty database, so a volume created under 61f055e keeps decimals DEFAULT 0 and the old seed rows — ./down.sh -v is the upgrade path. FRONTEND_REF moves with this pin (effectstream#918 is #63's UI half). THE SOLVER IMAGE IS THIS PIN TOO: 00011 PR B retired SOLVER_REF and rebuilt images/cow-solver as `FROM kernel-image` plus entrypoints, so there is no second solver commit anywhere in the stack (see $comment).",
      "supersededPin": {
        "ref": "61f055e83ae1ac4cb6e45031fb38c93c07f2d4d4",
        "branch": "main",
        "reason": "Phase H2's re-pin (main after kernel PR #60, Q14's NIGHT-decimals fix; itself superseding phase G's f92c7ca… and, before that, the feat/cow-solver branch pin 773e70cb…). Superseded by 00011 PR A once kernel PRs #61/#63/#66 merged five first-parent commits further on the same branch — `git merge-base --is-ancestor 61f055e… c293ebd…` exits 0, a pure fast-forward, so this re-pin drops nothing the earlier pins shipped against."
      },
      "provenance": {
        "branch": "main",
        "isBranchHead": true,
        "fulfilledPullRequest": 66,
        "note": "Kernel PR #66, \"offer poster: a give-size range\", is main's head at this pin (subject: \"Merge pull request #66 from effectstream/00027-offer-poster-size-range\"). The pin's REASON is PR #63 (\"every token has 6 decimals, faucets mint whole coins\") together with #61; #66 rides along because this entry stays pinned to main's head, as phase G's and H2's entries already established. Re-verified 2026-09-03 in a fresh clone: origin/main's head is exactly this SHA, and kernel PR #65 (ledger-v9) is still OPEN, so main is still the v8 line."
      },
      "$comment": [
        "THE SOLVER IMAGE IS THIS PIN. There is no sources[cow-solver] entry any more: 00011 PR B rebuilt",
        "images/cow-solver as `FROM kernel-image` plus four entrypoints (solver, solver-frontend,",
        "solver-provision, maker-offer), so every solver-side container reports THIS commit in",
        "/app/.kernel-commit and carries no second commit file at all.",
        "",
        "WHY THE OVERLAY WENT. SOLVER_REF used to pin kernel PR #52 (c37bfa68…), sound only while it was a",
        "DESCENDANT of KERNEL_REF. Kernel PR #48 merged the solver line into main and INVERTED that: the old",
        "pin became an ANCESTOR, 44 commits behind, so the overlay REVERTED 25 solver files while leaving the",
        "7 newer ones in place — a mixed tree running the PR #52 solver without #53's capital-free fee sizing",
        "and without #58's status listener. At this pin it also stopped BUILDING: the base image carries",
        "workspace packages the PR #52 lockfile has never seen (packages/solver-frontend, the poster's",
        "deploy/scripts/lib/poster-*.ts), so `bun install --frozen-lockfile` refused (measured 2026-09-03).",
        "00011 PR A moved SOLVER_REF onto this SHA so `./up.sh --all` kept building; PR B deleted the",
        "mechanism, the knobs and the .solver-commit outright. scripts/verify-source-pins.sh now asserts the",
        "solver image's /app/.kernel-commit == KERNEL_REF and the ABSENCE of /app/.solver-commit.",
        "",
        "Ancestry gate run 2026-09-03 in a fresh clone of effectstream/zswap-offerfiles-kernel, after `git",
        "fetch origin --prune`, confirming c293ebd… is origin/main's actual head (subject: \"Merge pull",
        "request #66 from effectstream/00027-offer-poster-size-range\"):",
        "  * the solver image needs no separate ancestry check any more: it IS this commit.",
        "  * the prior KERNEL_REF (61f055e…, phase H2's pin) is an ancestor of the new one — a pure",
        "    fast-forward of 5 first-parent merges, nothing the prior pin carried is lost.",
        "  * the v8 line still holds AT THIS REF: root package.json has ledger-v8 x2 / ledger-v9 x0 and",
        "    packages/solver-core/package.json has ledger-v8 x1 / ledger-v9 x0. Kernel PR #62 put ledger-v9",
        "    on main by mistake and #64 reverted it; #65 re-opens it and is still OPEN at this pin.",
        "",
        "WHAT #63 CHANGES FOR THIS PROFILE, read out of the diff before building:",
        "  * packages/database/migrations/000-init.sql — known_tokens.decimals DEFAULT 0 -> 6, and the",
        "    seeded rows are NIGHT/SNIGHT/USDC/USDM all at 6. asset_prices is unchanged (bitcoin 77387,",
        "    ethereum 2393.28, usd-coin 0.999818, midnight-3 0.01918181, usdm-2 1.001), so a per-base-unit",
        "    WBTC price is exactly 0.077387 and WETH exactly 0.00239328.",
        "  * packages/node/api.ts POST /v1/known-tokens — `decimals` omitted now means 6, not 0. Every",
        "    registration path upstream sends it EXPLICITLY anyway; images/offerfiles-kernel/entrypoint-",
        "    token-names.sh now does the same (00011 PR A), mirroring deploy/images/kernel/entrypoint-",
        "    register-minted-tokens.sh.",
        "  * packages/contracts-midnight/mint-test-tokens.ts — MINT_AMOUNT is unchanged at 1_000_000_000",
        "    base units; it is now spelled coinsToBaseUnits(1000n, 6) and MEANS 1 000 whole coins.",
        "  * new packages/solver-core/amount.ts — DEFAULT_TOKEN_DECIMALS = 6, the one place the literal",
        "    lives upstream.",
        "",
        "WHAT #61 CHANGES, AND THE ONE THING IT BREAKS HERE: 000-init.sql now SEEDS a SNIGHT row at the",
        "PREVIEW contract's colour 793c29c9…. On an `undeployed` devnet that colour cannot exist — this",
        "stack deploys its own shielded-night contract and derives a different colour every time. Because",
        "known_tokens.name is UNIQUE and POST /v1/known-tokens upper-cases the name, this repository's own",
        "shielded-night-token-name one-shot would 409 on the seeded row and the stack's REAL sNight colour",
        "would stay unnamed. Since 00015 that one-shot PATCHES the seeded row with this stack's colour",
        "before it posts — images/shielded-night/sql/snight-registry-patch.sql, run with psql after the",
        "kernel is healthy, reports itself synced and the chain is past block 1 — and it no longer treats a",
        "409 as unconditional success. (Before 00015 the answer was an exit-75 protocol plus a DELETE in",
        "up.sh; both are gone, and nothing here deletes a registry row.) Recorded upstream-side in the",
        "organizer's issues/00012; upstream's own 000-init.sql comment prescribes exactly this hand patch."
      ]
    },
    {
      "id": "zswap-da-template",
      "role": "the zswap-da trading SPA, built from a frozen upstream template subtree",
      "acquisition": "anonymous-git-fetch",
      "repository": "https://github.com/effectstream/effectstream.git",
      "ref": "58ab921be5513b77937a37be86bf724a41888302",
      "subtree": "templates/zswap-da",
      "subtreeSha": "3ca1d56ffc29f03c73cf43432bdfeeaf3ab43c6b",
      "envVar": "FRONTEND_REF",
      "verifiedOn": "2026-09-03",
      "reason": "The head of effectstream's `midnight-1` branch — the line the owner maintains for midnight-node 1.x / ledger-v8 / @effectstream 0.1xx (Q14, user decision 2026-09-01). At this ref the template is still v8-native in BOTH lanes: contract (ledger-v8 8.1.0, midnight-js 4.1.1, compact-js 2.5.1) and wallet (@effectstream/{midnight-contracts,wallets} 0.104.0, whose closure carries no ledger-v9), and it still carries the browser-network URI fix at api.getMidnightConfig (effectstream#912) — so this repository applies NO patch at all, exactly as before. RE-PINNED IN 00011 PR A to the branch head after effectstream#918 (\"amounts are whole coins — token decimals from the registry\"), which is the UI HALF of kernel PR #63 and MUST move together with KERNEL_REF: the SPA now reads each token's decimals off GET /v1/known-tokens, scales displayed and submitted amounts by 10^decimals, registers minted colours with an explicit decimals, and mints 1 000 WHOLE COINS (parseWholeCoins('1000', 6) = 1_000_000_000 base units) where it used to mint 1 000 base units. #918's two commits touch only src/ and README.md — no package.json, no bun.lock, no .compact source, no build script, no vite config — so every assertion this image makes was re-checked against the new subtree BEFORE building and holds verbatim, and the compactc 0.31.0 toolchain and the committed contract manifest are unaffected. The subtree SHA is recorded separately and verified during the build, because the commit alone does not prove which bytes of it were extracted.",
      "provenance": {
        "branch": "midnight-1",
        "supersededRefs": [
          "9474871185fd1b2704a9487718eee9d88c4f8edc",
          "332503c8f9216143a8c805f2a0acbcfd39e5a21d",
          "8d21ebe12c3e9b03da629b3eb486c381c33505a0",
          "f20a38cfccd4f2dec5c886e5c8bb321712d37924"
        ],
        "supersededSubtreeShas": [
          "af6023edf0d79269457adfaf17d09c9ebd301698",
          "1f63d7eedc9a8aff729b7fe026486cb89cb618de",
          "b22cf08d7d7e9f9b9e794a085c3e50fe342dde4d",
          "99b871f7aac2e8dc2b3a2172990f1a82bca13360"
        ],
        "upstreamedFix": "effectstream/effectstream#912 — browser-network URIs (api.getMidnightConfig), merged 2026-09-02, the last patch this image carried",
        "forkPoint": "b267fa2e795fcd1bd8e295d4d9b8eb65814b11ee",
        "identityProof": "Subtree b22cf08d = af6023ed (9474871, byte-identical to the frozen ref 332503c8 plus the retired effectstream-1x-line.patch: package.json blob 822bd218…, bun.lock blob 1099e6f6…) + effectstream#912, which changes only src/services/api.ts by the same +32 lines the retired browser-network-urls.patch applied. Verified 2026-09-01/02.",
        "repinnedFor": "effectstream#918 (00011 PR A, 2026-09-03) — feat(zswap-da): amounts are whole coins, token decimals from the registry (the UI half of kernel PR #63). Previously effectstream#916 (phase G) — port(zswap-da): reference rate, offset and sponsorship threshold"
      },
      "$comment": "Upstream templates/** on main is FROZEN by effectstream 00016 FR-10; the midnight-1 branch is the 1.x line's own. Nothing in this repository pushes to either — the pin is read-only."
    },
    {
      "id": "shielded-night",
      "role": "the Shielded NIGHT dApp — the NIGHT ⇄ sNight wrapper contract, its Node-side deploy/verify harness, and the Vite/React page the `shielded-night` profile serves",
      "acquisition": "anonymous-git-fetch",
      "repository": "https://github.com/effectstream/shielded-night.git",
      "branch": "main",
      "ref": "f7fcefa7921bf2c3f634871f9ad3aa3a32251af0",
      "envVar": "SHIELDED_NIGHT_REF",
      "verifiedOn": "2026-09-03",
      "reason": "A first-party public repository ALREADY on this stack's line, which is why this profile needed no porting: @midnight-ntwrk/ledger-v8 8.1.0 (with a tree-wide `overrides` pin), midnight-js 4.1.1, compact-js 2.5.1, compact-runtime 0.16.0, dapp-connector-api 4.0.1, and a contract whose pragma pairs with compactc 0.31.1 — the same triple this repository's core runs (node 1.0.0 / indexer 4.3.3 / proof-server 8.1.0). The image fetches this commit with `git fetch --depth 1 origin <sha>` and asserts the v8 line in BOTH directions before building. NOTHING of this source is committed here.",
      "provenance": {
        "branch": "main",
        "isBranchHead": true,
        "mergeCommitOf": [
          9,
          11,
          12
        ],
        "note": "The MERGE COMMIT of effectstream/shielded-night#12 (\"lockfile regen on bun 1.4.0 + verify-deployment.ts --allow-unlocked\", project 00007 questions Q7/Q8/Q11), which is main's head. main also carries #9 (the three integration knobs, 5902a90) and #11 (preprod address). RE-PINNED IN PHASE H2 from phase G's pin (6d87db4, main after #11) — a pure fast-forward of one PR."
      },
      "$comment": [
        "THIS PIN IS THE ONE THING PHASE A DID NOT OWN, AND IT IS NOW SETTLED. The three",
        "knobs the profile is built on — `window.SHIELDED_NIGHT.<NETWORK>_ADDRESS` (a",
        "build-time-only address cannot serve a per-stack devnet), the `MN_*_URL` endpoint",
        "overrides (a deploy running INSIDE compose cannot reach the hard-coded 127.0.0.1",
        "defaults), and `MN_EXTERNAL_STACK=1` (so the UPSTREAM integration suite is the",
        "round-trip gate rather than a transcribed copy) — landed upstream in PR #9, which",
        "the owner merged on 2026-09-03 (`5902a90`). This repository still carries NO patch",
        "against shielded-night, and images/shielded-night asserts each knob is present in",
        "the pinned tree so a re-pin to a tree without them fails the BUILD.",
        "",
        "WHAT THE MOVE FROM 0b0a358 BUYS, beyond retiring a branch-head pin: PR #11 filled in",
        "`frontend/.env`'s PREPROD_ADDRESS (e354e672…), and the page's dropdown is generated",
        "from the addresses that file carries. The stack-hosted page therefore now offers",
        "PreProd alongside Preview and Local (undeployed) — the same build the hosted site",
        "serves. Nothing about the local profile changes: /config.js still injects only",
        "UNDEPLOYED_ADDRESS, which is the only network this stack deploys to.",
        "",
        "WHAT THE MOVE FROM 6d87db4 (phase G's pin) TO f7fcefa (PHASE H2) BUYS: PR #12, this",
        "project's own upstream deliverable (Q7/C, Q8/A, Q11/A) — both `bun.lock` files",
        "regenerated wholesale on bun 1.4.0 (lockfileVersion 2; this repository's minimum",
        "bun is therefore now 1.4.x, already satisfied — `images/shielded-night/Dockerfile`'s",
        "BUN_BASE moved to oven/bun:1.4.0 in phase G, ahead of this re-pin, and is unchanged",
        "here), and `scripts/verify-deployment.ts --allow-unlocked`: the lock state is",
        "reported, not failed, so this stack's deliberately-unlocked devnet contract",
        "(FR-016) no longer makes the strongest check in the profile read as a failure.",
        "images/shielded-night/entrypoint-verify.sh's `verify_keys()` was rewritten to call",
        "it with the flag and read the exit code directly, dropping the ~70-line stdout",
        "parse (see the entrypoint's own header comment)."
      ]
    },
    {
      "id": "intents-relay",
      "role": "the Midnight Intents relay and the phase1-native-swaps browser UI",
      "acquisition": "operator-local-clone",
      "privateRepository": "shieldedtech/midnight-intents-swaps",
      "subtree": "phase1-native-swaps",
      "ref": "061f4d3258e25b9f3a451b4b4358ed232349d96b",
      "envVar": "RELAY_REF",
      "sourceDirEnv": "RELAY_SOURCE_DIR",
      "leakPolicy": "no-bytes-committed",
      "verifiedOn": null,
      "reason": "The audited pin the PR #52 solver was proven against end to end. Recorded as an IDENTITY ONLY. The repository is private and this public repository carries none of its content, fetches it from nowhere, and deliberately has no repository URL here — an operator supplies their own clone through RELAY_SOURCE_DIR and up.sh verifies it sits at this exact commit with a clean tree before any build. verifiedOn is null on purpose: this executor made no attempt to read the private remote, so claiming a verification date would be a fabrication.",
      "$comment": [
        "Every rule this pin implies is enforced mechanically, not by convention:",
        "  * .gitignore ignores local/, the conventional place for the operator's clone;",
        "  * scripts/verify-no-private-source.sh fails on any committed byte of that source;",
        "  * scripts/lib/common.sh assert_relay_source() verifies the clone before a build;",
        "  * images built from it are never pushed to any registry.",
        "Bumping this pin requires re-running the P5 end-to-end driver — the solver pin above",
        "was proven against THIS relay commit and no other."
      ]
    }
  ],
  "toolchains": [
    {
      "id": "compact",
      "version": "0.31.0",
      "decision": "official-upstream-direct",
      "resolved": true,
      "requiredBy": "images/zswap-da — the frozen template's scripts/build-contract.ts pins COMPILER_VERSION 0.31.0 and its committed src/contract/manifest.json verifies all 16 outputs plus the source hash byte-for-byte",
      "repository": "midnightntwrk/compact",
      "releaseTag": "compactc-v0.31.0",
      "releaseId": 315357301,
      "reason": "RESOLVED AT P3/T3.3 (Q9). This is not a version this repository chose: the frozen template DICTATES it. scripts/build-contract.ts pins COMPILER_VERSION = \"0.31.0\" and src/contract/manifest.json records the sha256 of the contract source (6fde5f8e2cfc5d5559f1468f3997f72810aec3093c6a87a54036b9175dadd3f0 — byte-identical to kernel main's copy) and of all 16 generated files; `compact compile` is deterministic, so any other compiler fails that manifest. The compiler is also the right partner for the template's runtime pins: compact-runtime 0.16.0 / compact-js 2.5.1 / midnight-js 4.1.1, which the Midnight Preview matrix pairs with the Compact 0.31.x toolchain. Q9's original option table was enumerated against LFDT-Minokawa/compact and assumed a 0.30.0 pin was needed; both premises were wrong — see `compact-kernel` for the other half of the split. The asset is fetched and sha256-verified in the image rather than through the `compact` version manager, whose resolution is an unpinnable network lookup.",
      "assets": {
        "linux/amd64": {
          "name": "compactc_v0.31.0_x86_64-unknown-linux-musl.zip",
          "assetId": 408401778,
          "outerSize": 27444303,
          "outerSha256": "ddbb4bb227c6a9c67098e65663648d8647097404ae094fdc0abe660839ea3ee8"
        },
        "linux/arm64": {
          "name": "compactc_v0.31.0_aarch64-unknown-linux-musl.zip",
          "assetId": 408401776,
          "outerSize": 26746686,
          "outerSha256": "9ead84322fb0c64aac6f94f12cd01eb011c1f3d0321d9fabc6a52b419c3dd6c6"
        }
      },
      "verifiedOn": "2026-09-01",
      "$comment": [
        "TWO COMPACT TOOLCHAINS, ON PURPOSE. This entry is the FRONTEND half (0.31.0, images/",
        "zswap-da); `compact-kernel` is the kernel half (0.30.0, images/offerfiles-kernel). Same",
        "contract source on both sides, two compilers, one deliberate split — because each side's",
        "generated bindings are version-checked against ITS OWN compact-runtime at import time",
        "(0.16.0 here, 0.15.0 there). Conflating them is what these two separate entries prevent.",
        "",
        "THE RESIDUAL RISK THIS SPLIT CARRIES, stated rather than discovered: the contract is",
        "DEPLOYED by the kernel from a 0.30.0 compile, while the browser proves against ZK assets",
        "the kernel serves from that same 0.30.0 compile (GET /keys/*, /zkir/* — the browser's own",
        "compiled keys are never used; only managed/contract/index.js is). So the browser's 0.31.0",
        "bindings drive 0.30.0 proving artifacts. P3's browser mint / offer create / offer take are",
        "the measurement that settles it; on a verifier-key rejection the fallback is Q9 option D.",
        "",
        "midnightntwrk/compact (repo id 967499978) and LFDT-Minokawa/compact (id 1115336329) both",
        "publish this tag, and for compactc-v0.31.0 the assets are BYTE-IDENTICAL: same sha256 and",
        "same sizes on both Linux triples (LFDT asset ids 408401806 / 408401804, verified",
        "2026-09-01). midnightntwrk is recorded because it is where the `compact` toolchain manager",
        "itself is published — so it is what `compact update <v>` resolves against — and because",
        "the kernel image already pins there; one fewer distinct upstream in the stack.",
        "",
        "The same release also ships aarch64-darwin and x86_64-darwin zips. Those must never enter",
        "a Linux build stage, which is why forbiddenAssetSubstrings is applied to toolchain assets",
        "and asserted again in the Dockerfile."
      ]
    },
    {
      "id": "compact-shielded-night",
      "version": "0.31.1",
      "decision": "official-upstream-direct",
      "resolved": true,
      "requiredBy": "images/shielded-night — the pinned tree's package.json runs `compact compile +0.31.1` and its CI pins COMPACT_VERSION 0.31.1; the image recompiles `src/shielded-night.compact` with exactly that compiler and FAILS the build if the output is not byte-identical to the committed `src/managed/`",
      "repository": "midnightntwrk/compact",
      "releaseTag": "compactc-v0.31.1",
      "releaseId": 344923756,
      "reason": "A THIRD Compact toolchain in this repository, and like the other two it is DICTATED by the source it compiles, not chosen: shielded-night's `package.json` scripts read `compact compile +0.31.1 …` and its `.github/workflows/ci.yml` sets COMPACT_VERSION '0.31.1', with a reproducible-build job that recompiles into an emptied `src/managed/` and fails on any diff. That job IS the dApp's verifiability claim (the deployed contract's on-chain verifier keys are the committed ones), so this image reproduces it locally rather than trusting it (project 00007, question Q3, owner decision A). 0.31.1 also pairs with the tree's compact-runtime 0.16.0, which the generated bindings version-check at import time. The asset is fetched and sha256-verified in the image rather than resolved through the `compact` version manager, whose lookup cannot be pinned.",
      "assets": {
        "linux/amd64": {
          "name": "compactc_v0.31.1_x86_64-unknown-linux-musl.zip",
          "assetId": 457912387,
          "outerSize": 27444336,
          "outerSha256": "e291b4bab4d4e857707008f8b1c25c2b8e0c843f6c737d0ee6c0d9ac69a6bbfb"
        },
        "linux/arm64": {
          "name": "compactc_v0.31.1_aarch64-unknown-linux-musl.zip",
          "assetId": 457912389,
          "outerSize": 26738536,
          "outerSha256": "7c7e38581808779d2671687c3378017bcf2fb3111a192fd1253f3472012df549"
        }
      },
      "verifiedOn": "2026-09-02",
      "$comment": [
        "THREE COMPACT TOOLCHAINS, ON PURPOSE, and this is the third: 0.30.0 compiles the",
        "KERNEL's contract (images/offerfiles-kernel), 0.31.0 the zswap-da TEMPLATE's copy of the",
        "same source (images/zswap-da), and 0.31.1 the entirely different ShieldedNight contract",
        "(images/shielded-night). They are not interchangeable and must never be collapsed into",
        "one variable: each side's generated bindings are version-checked against ITS OWN",
        "compact-runtime at import time, and each side's committed manifest/artifacts were",
        "produced by exactly one of them.",
        "",
        "midnightntwrk/compact (repo id 967499978) and LFDT-Minokawa/compact (id 1115336329) both",
        "publish compactc-v0.31.1 and the Linux assets are BYTE-IDENTICAL: same sha256 and same",
        "sizes on both triples (LFDT asset ids 457912432 / 457912433, release id 344923791,",
        "verified 2026-09-02 against the release API's own `digest` field). midnightntwrk is",
        "recorded for the same reason as the other two entries — it is where the `compact`",
        "toolchain manager is published, and it is where this repository already pins.",
        "",
        "The same release ships aarch64-darwin and x86_64-darwin zips. Those must never enter a",
        "Linux build stage: forbiddenAssetSubstrings is applied to toolchain assets and the",
        "Dockerfile asserts it a second time."
      ]
    }
  ],
  "proofData": {
    "decision": "prewarm-volume",
    "reason": "Proof data is architecture-neutral and belongs in one persistent volume, not embedded in an OCI layer and not fetched independently per service. The 2.x sibling populates that volume from a hash-verified immutable warehouse GENERATION, but that generation is Ledger-9 specific (SRS K0-K19 + ledger-static-noarch-9.0.0) and no equivalent v8 generation exists today. So the volume is filled by a pre-warm one-shot instead: it pays the srs.midnight.network fetch once, before up.sh reports ready, rather than during the first browser mint mid-demo.",
    "strategy": "prewarm-one-shot",
    "volume": "proof-data",
    "generation": null,
    "integrityLevel": "binary-anchored-verification",
    "integrityCaveat": "CORRECTED AT P1/T1.3 AFTER MEASUREMENT — the earlier text here read 'these are not offline-verified bytes; the pre-warm trusts srs.midnight.network at fetch time'. That was wrong in BOTH directions and is replaced by what was actually observed. (1) The proof-server binary HASH-VERIFIES everything it downloads, SRS public parameters included: it logs 'this is not a trusted service, the data will be verified' and then 'verified correct' for every payload, against expected hashes compiled into ledger-8.1.0. So the warmed bytes are cryptographically checked, and the trust root is this repository's digest-pinned official image rather than a committed manifest — weaker than the 2.x audited generation's reviewable hash list, but very far from 'fetch-time trust'. (2) The corresponding real hazard is the opposite of the one previously recorded: a pre-warm that fetched payloads ITSELF (curl, wget) would write bytes NOTHING verifies, because the server only verifies on download and never re-checks a file it finds already present. Pre-filling this volume by URL would therefore REMOVE a property the stack has today. That is precisely why the warm one-shot runs the official proof-server binary against the volume instead of downloading anything of its own.",
    "integrityMeasurement": {
      "measuredOn": "2026-09-01",
      "measuredAt": "P1/T1.3",
      "coldWarmSeconds": 4,
      "coldWarmBytes": 34177024,
      "coldWarmFileCount": 18,
      "warmRerunSeconds": 2,
      "warmRerunDownloads": 0,
      "payloads": "SRS bls_midnight_2p10..2p15, zswap/9/{spend,output,sign}.{prover,verifier,bzkir}, dust/9/spend.{prover,verifier,bzkir}",
      "coverageGap": "The binary fetches only its startup working set. A contract-sized circuit needing a larger SRS k (k16+) is NOT warmed, and because the reader mounts this volume read-only that is an EROFS failure at prove time rather than a slow prove — measured: a proof server on an empty read-only MIDNIGHT_PP exits 1 with `Os { code: 30, kind: ReadOnlyFilesystem }`. Which k the offer-files contract needs cannot be known until a contract exists (P2/P3). Tracked as open question Q11.",
      "readerIsReadOnly": true,
      "writerService": "proof-warm"
    },
    "supersededBy": {
      "plannedDecision": "warehouse-data-generation",
      "note": "The user is preparing an audited v8 proof-data mirror/generation in parallel. When it lands, its initializer replaces this one-shot as a drop-in WRITER OF THE SAME VOLUME, so no consumer changes. At that point `generation` becomes a 64-hex content digest and `decision` becomes warehouse-data-generation; the verifier enforces that pairing in both directions so a half-migration cannot pass."
    },
    "midnightParamSourceMustNotBeGitHub": true
  },
  "retainedPaths": [
    {
      "id": "offerfiles-kernel-image",
      "decision": "source-build",
      "inArtifactNormalizationScope": false,
      "reason": "Application code with no reusable warehouse runtime artifact. Built from the `offerfiles-kernel` source pin above."
    },
    {
      "id": "zswap-da-image",
      "decision": "source-build",
      "inArtifactNormalizationScope": false,
      "reason": "Application code with no reusable warehouse runtime artifact. Built from the `zswap-da-template` source pin above."
    },
    {
      "id": "shielded-night-image",
      "decision": "source-build",
      "inArtifactNormalizationScope": false,
      "reason": "Application code with no reusable warehouse runtime artifact. One image, two runtime targets (the deploy/verify one-shot on the bun base, the nginx page server), built from the `shielded-night` source pin above with the Compact artifacts recompiled in-image by `compact-shielded-night`."
    },
    {
      "id": "cow-solver-image",
      "decision": "source-build",
      "inArtifactNormalizationScope": false,
      "reason": "Application code with no reusable warehouse runtime artifact, and NO SOURCE PIN OF ITS OWN since 00011 PR B: the image is `FROM kernel-image` plus four entrypoints (solver, solver-frontend, solver-provision, maker-offer), so it inherits the `offerfiles-kernel` source pin above together with that image's already-compiled Compact artifacts. The retired fetch-and-overlay stage is described in images/cow-solver/Dockerfile."
    },
    {
      "id": "relay-image",
      "decision": "source-build",
      "inArtifactNormalizationScope": false,
      "reason": "Application code from a PRIVATE repository. Built only from an operator-local clone, and never pushed to any registry."
    },
    {
      "id": "intents-ui-image",
      "decision": "source-build",
      "inArtifactNormalizationScope": false,
      "reason": "Same private source and the same rules as relay-image."
    },
    {
      "id": "celestia-image",
      "decision": "source-build",
      "inArtifactNormalizationScope": false,
      "reason": "A thin local runtime image around the two verified warehouse binaries recorded in components[]. The image is built; the executables inside it are not."
    },
    {
      "id": "postgres-image",
      "decision": "source-build",
      "inArtifactNormalizationScope": false,
      "reason": "pg_ivm is compiled in; no reusable warehouse runtime artifact exists."
    }
  ]
}
